    Извиняюсь за вмешательство. Но.
    Не обязательно нужен SVD, чтобы обращать плохообусловленную матрицу.
    Можно недопускать составление плохообусловленной матрицы.
    Один из методов: Гауссова нормализация переменных подстановкой вида:

    t = (x - M(x)) / sqrt(2 * D(x) / n)

    После такой подстановки очень сложно получить плохообусловленную матрицу
    (нормализация проводится до составления нормальных уравнений).
    Только вот само решение уже не будет таким простым.

    y(x) = A * x
    y(t) = B * t

    Прийдётся выражать A из B повторной подстановкой.

    y(x) = B * (x - M(x)) / sqrt(2 * D(x) / n) = A * x


Keep it simple, stupid.
#9
zvezdochiot, 2 окт 2020
